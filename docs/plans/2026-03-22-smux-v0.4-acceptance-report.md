# smux v0.4 Acceptance Report

**Date:** 2026-03-22
**Commits:** `48c0603..c163c5a` (9 commits)
**Base:** v0.3 acceptance commit `48c0603`

---

## Summary

v0.4 closes all deferred items from v0.3 review rounds: security hardening (7 audit findings), spec alignment (5 verification-gate deviations), quality improvements, and UI polish.

## Test Suite

| Metric | v0.3 | v0.4 | Delta |
|--------|------|------|-------|
| Rust tests | 167 | 167 | — |
| Frontend tests | 0 | 11 | +11 |
| Total | 167 | 178 | +11 |
| Consensus tests | 12 | 12 | — |
| Multi-verifier orchestrator tests | 3 | 3 | — |

## Automated Checks

| Check | Result |
|-------|--------|
| `cargo build --workspace` | PASS |
| `cargo fmt --check` | PASS |
| `cargo clippy --workspace --all-targets -- -D warnings` | PASS |
| `cargo test --workspace` | PASS (167 tests) |
| `npx tsc --noEmit` | PASS |
| `npm run lint` | PASS |
| `npm test` (Vitest) | PASS (11 tests) |

## Changes: 19 files, +1652 / -166 lines

### Security Hardening (Tasks 31-35)

| Task | Finding | Fix |
|------|---------|-----|
| 31 | C-3: Unauthenticated IPC | Socket `0o600` + `getpeereid()` UID validation, `libc` dep |
| 32 | C-2: CSP disabled | Restrictive CSP in tauri.conf.json (done in v0.3 security commit) |
| 33 | H-3: SafetyConfig not applied | `create_adapter_with_safety()` + server-side `max_rounds` cap |
| 34 | H-4: iframe same-origin | Superseded by Task 43 (native WebView) |
| 35 | H-1+M-2: Weak session ID + large messages | Full UUID v4, IPC limit 16MiB → 1MiB |

### Spec Alignment (Tasks 36-40)

| Task | Finding | Fix |
|------|---------|-----|
| 36 | VG-001: Sequential verifiers | `tokio::JoinSet` parallel execution |
| 37 | VG-002: Config fields missing | `defaults.consensus` + `agents.verifiers` in config |
| 38 | VG-003: VerifierVerdict fields | `adapter_name`, `result`, `duration_ms` aligned to spec |
| 39 | VG-007: Single verifier feedback | Combined output from ALL verifiers `[verifier[N]]\n...` |
| 40 | VG-005: Shared HealthMonitor | Per-verifier `HealthMonitor` with independent tracking |

### Quality (Tasks 41-43)

| Task | Fix |
|------|-----|
| 41 | Vitest + @testing-library/react, 11 component tests |
| 42 | `GeminiHeadlessAdapter::is_available()` — npx + binary fallback, daemon skips unavailable |
| 43 | Native Tauri `WebviewWindowBuilder` replaces iframe, localhost-only URL validation |

### Polish (Task 44)

| Task | Fix |
|------|-----|
| 44 | Panel order state + Cmd+Shift+S swap + layout persistence |

## Execution Path Closure Verification

| Feature | Producer | Consumer | Connected? |
|---------|----------|----------|------------|
| Parallel JoinSet verifiers | orchestrator spawns tasks | join_next collects | YES |
| Per-verifier duration_ms | `tokio::time::Instant` in spawn | `VerifierVerdict.duration_ms` | YES |
| Config consensus field | `config.rs` DefaultsConfig | CLI reads `config.defaults.consensus` | YES |
| Config verifiers field | `config.rs` AgentsConfig | CLI reads `config.agents.verifiers` | YES |
| Gemini is_available() | `gemini.rs` async probe | daemon skips unavailable | YES |
| Combined feedback | orchestrator collects all outputs | `build_planner_feedback()` receives joined | YES |
| Per-verifier health events | `verifier_healths[i].check()` | `HealthStateChanged { agent: "verifier[i]" }` | YES |
| Native WebView | `open_browser_window` Tauri cmd | `WebviewWindowBuilder::new()` | YES |
| Socket 0o600 | `set_permissions()` after bind | OS enforcement | YES |
| Peer UID validation | `getpeereid()` on accept | `continue` on mismatch | YES |
| UUID session IDs | `Uuid::new_v4().to_string()` | full 128-bit in all paths | YES |
| 1MiB IPC limit | `recv_message()` check | `IpcError::MessageTooLarge` | YES |

## Remaining Known Items

| Item | Status | Notes |
|------|--------|-------|
| VG-006: Multi-session _session_id filtering | Deferred | Single active session sufficient for v0.4 |
| Full drag-and-drop (free positioning) | Partial | Panel swap implemented; free-form positioning deferred |
| Sound on session complete | Deferred | Desktop notifications sufficient |
| Native macOS menu bar | Deferred | Keyboard shortcuts cover all actions |

## Conclusion

v0.4 resolves all security findings from the v0.3 audit (C-2, C-3, H-1, H-3, H-4, M-2, M-4) and all verification-gate spec deviations (VG-001 through VG-005, VG-007). 178 total tests (167 Rust + 11 frontend). All automated checks clean.

Pending: persona-based path testing results.
