# smux v0.2 Production CLI Implementation Plan

> **Goal:** Transform the v0.1 headless prototype into a production-grade CLI tool that can be installed via `brew install smux` and used daily by developers. tmux-level reliability.

**Architecture:** Add daemon (session ownership, attach/detach), config file, error recovery, logging, and package distribution. No GUI — pure CLI.

**Tech additions:** `daemonize`, `unix socket IPC`, `tracing-subscriber`, `toml` config, `clap` completion

---

### Task 10: Daemon Architecture — smux-daemon process

**Files:**
- Create: `crates/smux-daemon/Cargo.toml`
- Create: `crates/smux-daemon/src/main.rs`
- Create: `crates/smux-core/src/ipc.rs`
- Modify: `Cargo.toml` (workspace member)

**What:**
- `smux-daemon` binary: background process that owns PTY/adapter sessions
- Unix socket IPC at `~/.smux/smux.sock`
- Protocol: `ClientMessage` / `DaemonMessage` (from spec)
- `smux start` → spawns daemon if not running → creates session
- `smux attach <id>` → connects to daemon → streams events
- `smux detach` → disconnects client, daemon keeps running
- `smux list` → queries daemon for active sessions

**Tests:**
- IPC message serialization/deserialization
- Daemon starts and accepts connection
- Session creation via IPC
- Multiple client attach/detach

---

### Task 11: Config File (~/.smux/config.toml)

**Files:**
- Create: `crates/smux-core/src/config.rs`
- Modify: CLI to load config

**What:**
- Parse `~/.smux/config.toml` (TOML format from spec)
- Defaults for: max_rounds, planner, verifier, adapter, safety settings
- CLI flags override config file
- `smux init` command creates default config

**Tests:**
- Parse valid config
- Defaults when no config file
- CLI override precedence

---

### Task 12: Structured Logging with tracing

**Files:**
- Modify: `crates/smux-core/` — add `tracing::info!` / `tracing::error!` throughout
- Modify: `crates/smux-cli/` — init tracing subscriber
- Modify: `crates/smux-daemon/` — file-based tracing subscriber

**What:**
- `SMUX_LOG=debug smux start ...` for verbose output
- Daemon logs to `~/.smux/logs/smux-daemon.log`
- Session logs to `~/.smux/sessions/<id>/session.log`
- Structured JSON log format option

**Tests:**
- Log output captured in tests
- Log level filtering works

---

### Task 13: Error Recovery & Self-Healing

**Files:**
- Create: `crates/smux-core/src/health.rs`
- Modify: `orchestrator.rs` — integrate health checks

**What:**
- Agent stuck detection (no events for N seconds → kill + restart)
- Agent crash → auto-restart with `restore_state(latest_snapshot)`
- Daemon crash → session metadata survives in `~/.smux/sessions/`
- `smux recover` command — find orphaned sessions, offer resume

**Tests:**
- Stuck detection triggers after timeout
- Crash recovery restores to correct round
- Orphaned session detection

---

### Task 14: Safety Layers 2 & 3

**Files:**
- Create: `crates/smux-core/src/safety.rs`
- Modify: adapters to pass permission flags

**What:**
- Layer 2: Pass `--dangerously-skip-permissions` / `-a never` based on config
- Layer 3: Post-hoc `git diff --stat` audit after each round
  - Alert if >5 files deleted or >2000 lines changed
  - Suggest rewind on threshold breach
- Safety report in Control Mode

**Tests:**
- Permission flags correctly passed to child process args
- Diff audit detects large changes
- Threshold alerts fire correctly

---

### Task 15: README, License, and Distribution

**Files:**
- Create: `README.md`
- Create: `LICENSE` (MIT)
- Create: `.github/workflows/ci.yml`
- Create: `.github/workflows/release.yml`
- Create: `Formula/smux.rb` (Homebrew)

**What:**
- README: hero GIF, 30-second pitch, install instructions, usage, architecture diagram
- MIT license
- CI: test on macOS (ARM + Intel) on every push
- Release: `cargo build --release` → GitHub Release → Homebrew tap update
- `brew tap kwmin122/tap && brew install smux`

**Tests:**
- CI passes on clean checkout
- `smux --version` shows correct version
- `smux --help` renders correctly

---

### Task 16: v0.2 Acceptance Gate

**What:**
- Full test suite
- Manual e2e: `smux start` with real Claude + Codex (if auth available)
- `smux attach` / `smux detach` lifecycle
- Config file loading
- Error recovery smoke test
- `brew install` from tap
- Acceptance report

---

## Notes

- Each task gets verification-gate after completion
- No task proceeds until previous task's gate returns PROCEED
- Real provider testing (Claude/Codex) requires API auth — test with fake adapters first, real last
- Daemon is the hardest task — allocate most time there
