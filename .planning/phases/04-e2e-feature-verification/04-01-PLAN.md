---
phase: 04-e2e-feature-verification
plan: 01
title: "Split layout persistence + E2E verification"
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, E2E-04]
verification: "swift build exits 0"
---

# Plan 04-01: Split Layout Persistence + E2E Verification

## Gap Analysis

| Requirement | Status | Gap |
|-------------|--------|-----|
| E2E-01 (Browser panel) | Working | None — toggleBrowser + WKWebView render functional |
| E2E-02 (DOM snapshot) | Working | None — domSnapshot() executes real JS |
| E2E-03 (Detach/reattach) | Broken | splits always `[]`, no restoration logic |
| E2E-04 (AppleScript) | Working | None — 7 handlers registered and wired |

## Tasks

### Task 1: Implement split layout serialization in saveState()

**Why:** WorkspaceWindowController.saveState() hardcodes `splits: []`. Must walk NSSplitView hierarchy.

**Changes:**
- WorkspaceWindowController.swift: Add `serializeSplits()` helper
- Call it in `saveState()` to populate the splits array

### Task 2: Implement split layout restoration in restoreState()

**Why:** restoreState() only restores window frame. Must reconstruct splits from saved state.

**Changes:**
- WorkspaceWindowController.swift: Add split reconstruction logic in `restoreState()`
- Walk saved SplitState tree and call doSplit() to recreate layout

### Task 3: Build verification

- `swift build` exits 0
- All 4 E2E requirements verified via code review
